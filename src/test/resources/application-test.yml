spring:
  application:
    name: futsal-manager
  datasource:
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  flyway:
    enabled: false

  spring-doc:
    paths-to-match: /api/**/v1/**
    swagger-ui:
      use-root-path: true